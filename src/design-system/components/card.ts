import type { ComponentTokenRecord } from './types'

export const cardTokens: ComponentTokenRecord = {
  background: 'surface.card',
  foreground: 'text.primary',
  border: 'border.default',
  headerForeground: 'text.primary',
  headerBorder: 'border.subtle',
  descriptionForeground: 'text.secondary',
}
