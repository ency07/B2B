import type { ComponentTokenRecord } from './types'

export const tabsTokens: ComponentTokenRecord = {
  listBorder: 'border.default',
  triggerForeground: 'text.secondary',
  triggerHoverForeground: 'text.primary',
  triggerHoverBackground: 'surface.hover',
  triggerActiveForeground: 'text.primary',
  triggerActiveBorder: 'action.primary',
  contentForeground: 'text.primary',
}
