import type { ComponentTokenRecord } from './types'

export const filterBarTokens: ComponentTokenRecord = {
  background: 'surface.hover',
  foreground: 'text.primary',
  border: 'border.default',
  activeFilterBackground: 'surface.selected',
  activeFilterForeground: 'text.primary',
}
