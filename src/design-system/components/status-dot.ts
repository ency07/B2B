import type { ComponentTokenRecord } from './types'

export const statusDotTokens: ComponentTokenRecord = {
  successColor: 'status.success',
  warningColor: 'status.warning',
  errorColor: 'status.error',
  neutralColor: 'text.muted',
  pendingColor: 'status.info',
}
