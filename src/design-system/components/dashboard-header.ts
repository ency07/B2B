import type { ComponentTokenRecord } from './types'

export const dashboardHeaderTokens: ComponentTokenRecord = {
  background: 'surface.background',
  foreground: 'text.primary',
  border: 'border.subtle',
  iconForeground: 'icon.default',
  searchBackground: 'surface.hover',
  searchForeground: 'text.primary',
  searchPlaceholder: 'text.muted',
}
