import type { ComponentTokenRecord } from './types'

export const emptyStateTokens: ComponentTokenRecord = {
  iconForeground: 'icon.secondary',
  titleForeground: 'text.primary',
  descriptionForeground: 'text.secondary',
  actionBackground: 'action.primary',
  actionForeground: 'text.inverse',
}
