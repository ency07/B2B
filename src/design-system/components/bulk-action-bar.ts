import type { ComponentTokenRecord } from './types'

export const bulkActionBarTokens: ComponentTokenRecord = {
  background: 'action.primary',
  foreground: 'text.inverse',
  countForeground: 'text.inverse',
  buttonBackground: 'surface.hover',
  buttonForeground: 'text.primary',
  buttonHoverBackground: 'surface.active',
  border: 'border.default',
}
