import type { ComponentTokenRecord } from './types'

export const splitDetailTokens: ComponentTokenRecord = {
  panelBackground: 'surface.card',
  panelForeground: 'text.primary',
  panelBorder: 'border.default',
  dividerBackground: 'border.subtle',
  headerForeground: 'text.primary',
  labelForeground: 'text.secondary',
  valueForeground: 'text.primary',
}
