import type { ComponentTokenRecord } from './types'

export const themeCustomizerTokens: ComponentTokenRecord = {
  background: 'surface.card',
  foreground: 'text.primary',
  border: 'border.default',
  selectedBorder: 'action.primary',
  previewBackground: 'surface.hover',
  labelForeground: 'text.secondary',
}
