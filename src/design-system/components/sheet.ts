import type { ComponentTokenRecord } from './types'

export const sheetTokens: ComponentTokenRecord = {
  overlayBackground: 'surface.overlay',
  contentBackground: 'surface.modal',
  contentForeground: 'text.primary',
  headerForeground: 'text.primary',
  descriptionForeground: 'text.secondary',
  border: 'border.default',
  closeForeground: 'icon.default',
  closeHoverBackground: 'surface.hover',
}
